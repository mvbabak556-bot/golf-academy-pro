# 🏌️ آکادمی گلف ۱۴۰۵ — داشبورد PRO

داشبورد حرفه‌ای مدیریت آکادمی گلف با **دنیای سه‌بعدی** — طراحی شده برای سال ۱۴۰۵ (۲۰۴۰).

## 🌐 لینک زنده (GitHub Pages)
سایت در ریشهٔ همین ریپو (index.html) قرار دارد و از GitHub Pages سرو می‌شود.

## 🔑 ورود
| نام کاربری | رمز |
|---|---|
| `admin` | `golf1405` |
| `coach` | `golf1405` |
| `manager` | `golf1405` |

## ✨ امکانات
- 🌍 **دنیای سه‌بعدی** — بعد از ورود از منو «دنیای سه‌بعدی» را بزنید؛ با کلیک روی هر ساختمان/آواتار دوربین به‌صورت سینماتیک وارد آن بخش می‌شود:
  - بازیکن‌ها → پروفایل سه‌بعدی با هندیکپ و امتیاز واقعی
  - مسابقات → پنل‌های هولوگرام با تاریخ فارسی و شمارش معکوس
  - سکوی قهرمانی، بارهای رده‌بندی، تیکر زنده، میدان نبرد دو تیم
- 📊 ۱۳ صفحهٔ تحلیلی: فرماندهی، رقابت فصل، مرکز بازیکن، فرماندهی مسابقه، هوش زمین، رکوردها، تقویم، تلویزیون، میدان نبرد، آکادمی + ابزار طراح
- 🔒 ورود با نام کاربری/رمز، طراحی فارسی، برند سبز/طلایی، کاملاً واکنش‌گرا
- 📦 نسخهٔ تک‌فایلی (آفلاین) — همهٔ استایل/اسکریپت/تصاویر در یک فایل

## 🗂 ساختار
```
index.html            ← نسخهٔ تک‌فایلی (سرو شده توسط Pages)
source/
  ├── index.html      ← نسخهٔ توسعه (چندفایلی)
  ├── css/ js/ assets/
  ├── build_standalone.py   ← ساخت نسخهٔ تک‌فایلی
  └── test_world_smoke.js   ← تست خودکار دنیای سه‌بعدی
```

## 🔄 بازسازی نسخهٔ تک‌فایلی
```bash
cd source
python3 build_standalone.py
```
